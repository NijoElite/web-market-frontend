import styled from '@emotion/styled';
import { Block } from '../../components/Block/Block.component';

export const ActionsBlockStyled = styled(Block)`
  padding: 15px;
  margin-top: 15px;

  > * {
    margin-right: 15px;
  }
`;
